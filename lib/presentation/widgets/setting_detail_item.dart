import 'package:flutter/material.dart';

class SettingDetailItem extends StatelessWidget {
  const SettingDetailItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
